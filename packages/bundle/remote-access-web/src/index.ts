/**
 * @froststarinquire/dsh-remote-access-web — the dsh browser-surface remote-access
 * bundle. The package's substance is `cordis.patch.yml`, declared by the
 * `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; the patch inserts the reverse-tunnel host row over the
 * dsh-web-app surface and pins the directory-picker seam to the in-app browse
 * interaction so a remote operator can add workspaces. This module carries no
 * runtime API.
 * @module @froststarinquire/dsh-remote-access-web
 */

export {}
